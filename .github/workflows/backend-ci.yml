name: Deploy to AWS EC2

on:
  push:
    branches:
      - main  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: List SSH Keys
        run: ssh-add -l

      - name: Test SSH Connection
        run: |
          ssh -o ConnectTimeout=60 -o StrictHostKeyChecking=no ubuntu@13.60.163.228 'echo "SSH Connection Successful!"'

      - name: Deploy to EC2
        run: |
          timeout 300s ssh -v -o ConnectTimeout=60 -o StrictHostKeyChecking=no -T ubuntu@13.60.163.228 << 'EOF'
          mkdir -p ~/.ssh
          ssh-keyscan -H 13.60.163.228 >> ~/.ssh/known_hosts
          sudo lsof -t -i:4000 | xargs kill -9 || echo "No process using port 4000"
          cd /home/ubuntu/projects/random/Backend_Part_Nestjs
          git pull origin main
          npm cache clean --force
          npm install --verbose
          npm run start -- --port 4000
          EOF

      - name: Reload Nginx
        run: |
          timeout 60s ssh -o ConnectTimeout=60 -o StrictHostKeyChecking=no -T ubuntu@13.60.163.228 << 'EOF'
          sudo systemctl reload nginx
          EOF
